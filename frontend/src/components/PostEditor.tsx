import React from 'react';

export const PostEditor: React.FC = () => {
  // TODO:
  // - body_text input
  // - title (optional)
  // - post_type selector
  // - media upload
  // - provider/account selector (most restrictive constraints)
  // - character count, validation errors
  return (
    <div>
      <h1>New Post</h1>
      <p>Post editor stub. Implement form, validation, and preview workflow.</p>
    </div>
  );
};
